{
  "name": "botwin-renovations-website",
  "version": "1.0.0",
  "private": true,
  "description": "Website for Botwin Renovations LLC \u2014 renovation & remodeling.",
  "scripts": {
    "build": "node build.js",
    "start": "node build.js && node server.js",
    "serve": "node server.js",
    "images": "node scripts/optimize-images.mjs",
    "test": "node build.js && node scripts/check.js",
    "preview-file": "node build.js && node scripts/build-preview.js",
    "deck": "node scripts/build-deck.js"
  },
  "engines": {
    "node": ">=18"
  }
}
